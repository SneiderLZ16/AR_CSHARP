namespace SkyTravel.Web.Models
{
    public enum FlightStatus { Programado, EnVuelo, Finalizado, Cancelado }
    public enum ReservationStatus { Activa, Cancelada, Completada }
    public enum TicketStatus { Generado, Error }
}
